# Cloud-daemon deps. Kept minimal so the Fly container stays small
# and rebuilds are fast. Pinned majors only — patch versions float
# so security fixes flow without manual bumps.
anthropic>=0.40
httpx>=0.28.1
pyyaml>=6
fastapi>=0.110
uvicorn[standard]>=0.30
