# ArchHub Roadmap — single source of truth

> **This is the one roadmap — and per founder mandate (2026-05-18) it
> stays the only one.** Every plan, backlog item and milestone lives
> here; no parallel roadmap or plan file may be created. It is also the
> machine-readable seed for the autonomous loop
> (`agents/roadmap_source.py`). Each `- [ ]` line is an OPEN backlog
> item the dispatcher picks up; `- [x]` is shipped.
> Tag priority `#P0` / `#P1` / `#P2`. Department hint in parentheses
> routes the item — `eng`, `qa`, `docs`, `ops`, `rnd`.
>
> When an item ships, MOVE it to "Done — last 7 days"; the loop records
> its hash in `agents/state/completed_roadmap_ids.txt` on the next tick.
>
> Consolidated 2026-05-18 from 6 scattered docs. The product-version
> history (the old root `ROADMAP.md`) is folded into "Shipped" below.
> The four design/architecture memos are NOT roadmaps — they are listed
> under "Design references" and kept for rationale only.

## AgDR ledger — reconciliation (2026-05-26)

**48 AgDRs** in `docs/agdr/` (regenerated 2026-05-26 from frontmatter scan).
State by range:

- **AgDR-0001-0011** — node-system redesign slices.  All `executed`
  (including 0005, 0006 — both flipped after group collapse / nesting
  shipped in later slices).
- **AgDR-0012** — architecture lock Direction X.  `partially_superseded`:
  ReactFlow-substrate clause → AgDR-0048 · LIBRARY-FIRST details →
  AgDR-0013/0014 · Speckle wire details → AgDR-0016.
- **AgDR-0013, 0014, 0016, 0018-0021** — `executed` (library-first
  enforcement, design system, speckle adapter router, adapter batch 2,
  typed-AI nodes, node-to-code, ai.plan canvas node).
- **AgDR-0015, 0017, 0022** — `proposed` (visual UI design system,
  revit speckle ops, reactflow scaffold).  0022 is moot — superseded
  in spirit by AgDR-0048 which adopts custom canvas as substrate.
- **AgDR-0023** — `superseded by AgDR-0025` (revitmcp roslyn isolation
  rolled into the broader connector-wide AgDR-0025).
- **AgDR-0024-0036** — 13 AgDRs (host-node v2, roslyn isolation
  all-connectors, cold-start lag, hot-reload, library actions, build
  pipeline shim, csc probe, archhub-mcp, composer stream coalesce,
  skill tombstone delete, daily maintenance, bridge slot non-blocking,
  non-blocking slot mechanism).  All `approved` — functionally shipped
  + CDP-verified, but status flip `approved → executed` is **pending
  founder signoff** per AGDR MANDATE (bulk reconciliation task on the
  AgDR-0047 workshop backlog).
- **AgDR-0037** — `executed`.  Was a second AgDR-0032 (id collision);
  renumbered 2026-05-22.  Documents ScriptCompiler .NET 8 host
  viability.
- **AgDR-0038-0042** — `executed` (composer capability nodes,
  default-screen home & splash, modular logic nodes, graph robustness,
  shared-memory knowledge graph).
- **AgDR-0043** — `proposed`: workshop · deliver the surface.
- **AgDR-0044, 0045** — `executed` (personal-brain-mcp daemon,
  settings × brain unified).
- **AgDR-0046** — `proposed`: brain-settings rebuild workshop.
  Pending founder signoff; blocks BrainSection JSX.
- **AgDR-0047** — `proposed`: repo overhaul workshop.  Pending founder
  signoff on F1-F5 forks; blocks 11-slice plan including the perf
  slices + dead-JSX removal.
- **AgDR-0048** — `executed`: supersedes AgDR-0012's ReactFlow lock +
  AgDR-0022 in full.  Renumber chain 0045→0046→0048.

## Shipped — milestone arc

| Milestone | What |
|---|---|
| v0.25–v0.27 | Connector self-heal · Outlook COM · Studio 3-pane shell · brand v0.1 · Revit multi-session broker |
| v0.28–v0.33 | Add-Host wizard · workflow node canvas · marketplace · ⌘K palette · parameters sidebar |
| v0.34.x | Voice · telemetry KPIs · reasoning surfacing · multi-host polish (15 point releases) |
| graph-first pivot | WorkspaceShell replaces the page-based shell; Session = Graph; canvas is the primary surface (`studio-lm.jsx`) |
| connectors | 16 host/office/broker connectors on a uniform base contract |
| cloud backend | companies / multi-seat · admin dashboard · welcome email · per-company quota · invite email-match |
| release artifacts | Windows installer · macOS `.icns` + `.dmg` · Linux AppImage |
| repo hygiene | domain canonicalized to archhub.io · roadmap docs consolidated · 1323 tests green |

Per-version detail: `CHANGELOG.md` and git history.

## NEXT 7 DAYS

- [ ] #P0 Push the repo to GitHub — CI (AppImage / macOS / test / CodeQL / Dependabot / daily-audit) is unverified and inert until the default branch is pushed (ops)
- [x] #P1 AgDR-0035 + AgDR-0036 — UI-freeze class KILLED. get_all_hosts / get_local_llms / probe_connector / list_host_sessions / list_host_documents / get_memory_stats / list_memory_facts / get_storage_stats all routed through `_cached_async` (cached + bounded pool + signal); `_startup_self_test` moved to a daemon thread. Guard `tests/test_no_blocking_slots.py` fails CI on any new blocking slot. (eng)
- [ ] #P1 AgDR-0036 leak-class follow-ups — found by the 2026-05-22 deep audit, NOT the freeze class (slow degradation, not instant freeze): (a) `GraphTriggerScheduler` — `disarm`/`stop` nulls `_thread` even when the 2s join times out → next `arm` spawns a 2nd live loop; never stopped on app exit. Fix: don't null on timeout, refuse re-`start` while alive, stop on exit; (b) uncapped raw `threading.Thread` spawns in `run_connector_op` / `request_param_options` / `agent_step` / `ai_create_node` / `delete_saved_skill` cloud-push — route through the shared `_async_state` pool; (c) `WorkflowRunner` `wire_bus` / `node_outputs` grow unbounded + no guard against two cooks interleaving — cap/clear after `run_all`, serialise `run_workflow`/`run_node` with a lock; (d) `ArchHubBridge.__init__` runs `load_all_connectors` + `custom_nodes.load_all` synchronously before first paint — defer to a daemon thread; (e) DevTools `QWebEngineView` never `deleteLater`d — dispose on hide; (f) `NodeCanvas` rebuilds wire/group geometry every render — wrap in `useMemo`; (g) tool_engine connector-op `__` name encoding not invertible for op_ids containing `__` — carry the raw op_id in the schema (eng)
- [x] #P0 Graph-first canvas "Run" — RESOLVED 2026-05-18 by the NODE-SYSTEM REDESIGN (`normalize_canvas_graph` adapter — canvas graphs cook end-to-end, CDP-verified live). Original diagnosis kept for history: it was broken for the curated node library — the 80 `LM_LIBRARY` node types (`LM_NODE_TEMPLATES`, `studio-lm.jsx:1846`) carry NO engine `type` field; `bridge.run_workflow`/`run_node` pass the raw canvas graph to `WorkflowRunner`, which dispatches on `node.type` → empty → `registry` miss → `"no executor for ''"` (`runner.py:475`). A user can drag, wire and Run any curated library node and it errors. (Connector-op nodes carry `op_id`, custom nodes carry `custom_type`, conversation chat uses a separate path — those 3 DO work; the 80-node curated palette does not.) Investigation 2026-05-18 found it is deeper than a missing field: of the 80 `LM_LIBRARY` nodes only ~30 have a real engine executor (7 `host.*`, `control.if/merge/foreach`, `conversation.chat`, `llm.classify`, connector-op-backed reads). The other ~50 — most `filter`/`transform`/`compose`/`annotate` nodes, the 11 non-engine host nodes (word/excel/teams/notion/…), `logic` switch/loop/split/delay/throttle, several `ai` nodes — have NO engine executor at all. `LM_LIBRARY` is an aspirational catalogue (`docs/NODE_LIBRARY_v2.md`) the engine never caught up to. Fix is multi-slice: (1) binding adapter; (2) wire the ~30 nodes that have executors + verify graphs cook; (3) the ~50-node executor gap — build the missing executors (default — make it work) or trim the library. Same parallel-registry drift class as connectors/tool_engine.
  - SLICE-1 SPEC (verified 2026-05-18): the binding is a multi-field ADAPTER, not a `type` stamp. Canvas nodes carry `cat` + `id` + `params` (a list of `{k,v,type}`); `runner.py` dispatches on `node.type` and reads `node.config` (a dict) — it reads neither `cat` nor `params` (verified `runner.py:380,425,481`). Engine types confirmed: `host.{family}`, `conversation.chat`, `control.if/merge/foreach`, `llm.complete/complete_with_tools/classify`, `aec.*`, `io/output.parameter`, `data.constant`, `subgraph.*`. Slice 1 = a canvas-node→engine-node normaliser (in `addNodeFromLibrary` or bridge-side) that stamps `type` from a verified `id`→`type` map AND folds `params`→`config`; then a runner test proving a `control.if` / `conversation.chat` graph cooks end-to-end. Half-implementing (type only, no config) makes nodes "run" with empty config — looks fixed, isn't — so it ships as one correct adapter, not a partial stamp (eng)
- [ ] #P0 Bridge slots block the Qt main thread — boot-hang, observed LIVE 2026-05-18: a clean relaunch hung on the boot splash ~8 min (backend self-test finished in seconds per `boot.log`; UI never rendered; `pythonw` idle at 7s CPU = blocked, not spinning). A 2nd relaunch of identical code booted fine → non-deterministic. Cause: sync work on the Qt main thread — `get_models` (Ollama + LM Studio HTTP), `probe_connector` (COM/HTTP probe), and the startup slot prefetches in `index.html`; when a host is slow/unresponsive the main thread blocks and the QtWebEngine UI never completes its bridge handshake. Boot hangs forever on some fraction of launches → P0 (was #P1). Fix: every slow `@pyqtSlot` (probes, LLM, COM/HTTP) + every startup prefetch runs on a worker thread + signal, mirroring `run_connector_op` — never block the Qt main thread (CLAUDE.md rule) (eng)
- [ ] #P0 NODE-SYSTEM REDESIGN — node grammar + UI/UX (founder mandate 2026-05-18). The current ~80-node library is decorative and mostly non-functional; redesign around a SMALL set of primitive/master nodes with ComfyUI / n8n / Grasshopper / Dynamo-grade UX. Founder intent (a guideline, NOT a literal spec — research + design, apply judgment): nodes simple, modular, composable from each other with logic. Primitive families — INPUTS (multiple nodes, optimum input control); OUTPUTS (multiple nodes, optimum output control); WATCHERS & TRIGGERS (view / list / model / image / …); CONNECTORS (ONE master node per host — every tool + data call exposed as right-panel parameters, no scattered per-op host nodes); AI (ONE master node — every AI action as a parameter). From these primitives, generate other nodes. Save-as-Skill → the skill is itself a usable node (recursive). The selected node's parameters render in the right-side panel. Folds in the canvas-Run #P0 above — build executors for the NEW small node set, not the old 80. Design: `docs/NODE_GRAMMAR.md` (written 2026-05-18 — the principle, the ~12-primitive grammar, skill-as-node, 10 build slices). Loop until grammar + UI/UX shipped and working — zero decorative nodes. (eng)
  - SLICE 1 ✓ SHIPPED (b241d07) — one node model: `node_grammar.normalize_canvas_graph` stamps engine `type` + `config` onto canvas nodes; `run_workflow`/`run_node` cook a real graph. CDP-PROVEN 2026-05-18: a `constant→output` grammar graph cooks `42` in the running app.
  - SLICE 2 ✓ SHIPPED (1143dc9) — `connector.run` master executor: one node runs any host op (`host` + `op` params); folds the connector path into the runner.
  - SLICE 3 ✓ SHIPPED — `ai` master node: grammar `ai` primitive with the `action` param (chat/complete/classify/tools) resolving to `conversation.chat` / `llm.*`.
  - ✓ SHIPPED also: `get_node_grammar` bridge slot · JSX palette swap (`◆ NODES · 12`, replaces the 80-node `LM_LIBRARY`) · `filter`/`transform`/`watch` executors · grammar declares every primitive's params + ports. ~1208 desktop tests green; place→configure→cook chain CDP-verified live.
  - SLICE 4 ⚠ SHIPPED 2026-05-18 WITH WRONG SHAPE → superseded by SLICE A. The cleanup half was correct (killed the duplicate `⚡ CONNECTORS · 16 hosts` 116-op palette, deleted the `LM_LIBRARY`/`PIPELINE` defs + all refs, retired `ConnectorOpRail`, rebuilt `NodeLibrary` + `WirePromotePalette` on the grammar; root-cause fix: `NodeRail` was handing rails a shallow-copy node, `ConnectorRail` now resolves the real `LM_GRAPH` node) — but the master node shape was WRONG: one universal `Connector` with a host dropdown, when the founder's intent (and ROADMAP line above) was "ONE master node per host" → 16 host master nodes (Revit / Excel / AutoCAD / Outlook / …). Cause: `docs/NODE_GRAMMAR.md` §4 was self-contradictory and Claude resolved it silently. Brutal lesson logged → AgDR mandate added to `CLAUDE.md`, AgDR-0001 written. CDP-proof of the cleanup half is still valid (Excel `read_range` cooked 36 rows × 11 cols live in 56ms); only the palette/inspector shape changes in SLICE A.
  - **NEXT — slices A–G, locked design in `docs/agdr/AgDR-0001-node-system-redesign.md`** (sourced from grounded research into Grasshopper / Dynamo / ComfyUI / n8n, 2026-05-20):
    - [x] **SLICE A ✓ SHIPPED 2026-05-20** — Connector reshape: palette now emits 16 per-host master nodes from `LM_CONNECTORS` (Microsoft Word / Excel / PowerPoint, Outlook, Adobe Photoshop / Illustrator / InDesign, Speckle, Notion, Dropbox, Microsoft Teams, Blender, Rhino, Autodesk Revit / AutoCAD / 3ds Max) — `host` LOCKED per palette entry via `_host` on the palette item; `addNodeFromLibrary` `_grammar` branch consumes `_host` to seed `node.host` + `node.config={host,op:''}` and skip the template host/op param rows. `ConnectorRail` adapts: host shown as a read-only BADGE (with mechanism: BROKER / COM / HTTP) instead of a picker; OPERATION picker + typed params unchanged; legacy host picker preserved only for nodes from saved graphs with no host. Engine path unchanged (one `connector.run` executor). CDP-PROVEN live 2026-05-20: place "Autodesk Revit" → `kind:'connector'`, `host:'revit'`, no host picker, 13 Revit ops in the OPERATION dropdown (`revit.list_views`, `revit.list_walls`, …). Palette text confirmed: `◆ NODES · 27` = 11 primitives + 16 host masters.
    - [x] **SLICE B (disable verbs + Pin) ✓ SHIPPED 2026-05-20** — design AgDR-0002. Four independent composable booleans on the node — `bypass` / `frozen` / `preview_off` / `pinned` (+ `pinned_value` / `pinned_at`). Engine effect via `normalize_canvas_graph` graph rewriting: PIN and FREEZE replace the node's `type` with `data.constant` carrying the snapshot/cached value (and strip the source flags so the runner's existing `frozen` short-circuit doesn't second-guess the rewrite); BYPASS removes the node and rewires its first inbound wire to its first outbound wire. Document-level keydown handler in `StudioLM` for the focused node: `Ctrl+B`/`Ctrl+F`/`Ctrl+Shift+P`/`P`. `lm-run-connector-op` short-circuits on the verbs too. Visuals: dashed border (bypass), cyan tint (frozen), 0.55 opacity + ⊘ (preview-off), 📌 + timestamp (pin), each rendered as title-bar indicators. +9 tests covering pin/freeze/bypass rewrite + end-to-end cook + preview-off-is-engine-no-op + pin-wins-over-freeze + backward-compat. 57 tests green. CDP-PROVEN live: place Constant → focus → dispatch each verb → state and visuals correct.
    - [x] **SLICE B2 ✓ SHIPPED 2026-05-20** — Multi-select + canvas ergonomics. Design AgDR-0003. `selectedIds: Set<string>` state in `NodeCanvas` alongside `focusId`. Plain click on a node: replace selection. Shift/Ctrl-click: toggle id in selection (no drag started). Shift+empty-canvas-drag: rubber-band — band rect renders dashed accent overlay; on mouseup every node whose bbox intersects the rect joins the selection. Plain click on an already-selected node: multi-drag — cursor delta applies to ALL selected positions in lockstep. `Alt` held during node drag: push-neighbours — non-selected nodes overlapping the dragged bbox get shoved aside by the overlap amount in the drag direction (with a 12px gap). `Ctrl+Shift+U` / `Ctrl+Shift+D` walks `LM_GRAPH.wires` one hop and expands the selection upstream / downstream. `Escape` clears the selection. Delete key now nukes ALL selected nodes (not just focusId). Selected nodes render a 2px accent ring (focused-ring still wins visually). 57 tests green (no regressions). CDP-PROVEN live 2026-05-20: place Constant + Input + Output → plain-click first, shift-click 2nd + 3rd → all 3 have the selected outline; wire `constant→output`, select output, `Ctrl+Shift+U` → constant joins the selection. Known nit (not slice B2): rapid sequential placement collides node IDs (`Date.now().toString(36).slice(-4)` only varies by ms) — separate small follow-up.
    - [x] **SLICE C (Groups MVP) ✓ SHIPPED 2026-05-20** — design AgDR-0004. `LM_GRAPH.groups: [{id, title, description, style, nodeIds[]}]` round-trips through `save_graph` (no Python change). Six predefined Group Styles (INPUT pink / CONNECTOR orange / AI purple / TRANSFORM cyan / OUTPUT green / NOTE grey). `Ctrl+G` opens a `GroupDialog` modal — title input + style picker pill row + Create/Cancel. Groups render BEHIND nodes as a dashed-border rectangle auto-sized to members' bbox + 16px padding + 28px header; header shows style badge + title + node count + ungroup ✕. Drag the header → multi-drag all members (reuses slice B2 infra). Click body → select all members. CDP-PROVEN live 2026-05-20: place Constant + Input → multi-select → `Ctrl+G` → dialog opens with 6 style pills → Create → group renders on canvas with dashed cyan TRANSFORM border, 252×172, header reads "TRANSFORM · Group · 2 nodes · ✕". 57 tests green, no regressions.
    - [x] **SLICE C2 ✓ SHIPPED 2026-05-21** — Group collapse to giant-node. Design AgDR-0005. Pure-visual + engine-time wire-rewrite mechanism. Boundary-port auto-promotion: a member-node port is BOUNDARY when (a) no wire OR (b) wire's counter-end node ∉ group → boundary inbound/outbound ports become `<gid>:in|out:<i>` sockets on the collapsed group-node. Python `expand_collapsed_groups()` pre-pass in `normalize_canvas_graph()` rewrites every wire endpoint referencing a group-socket id back to the underlying `(memberId, portName)` — runner sees the same FLAT graph collapsed or expanded; `_promoted_ports_for()` deterministic ordering = traversal over `nodeIds` × engine port order. JSX mirror `promotedPortsForGroup()` + `groupViews`/`memberAlias`/`groupSocketAlias` resolve every wire endpoint to a screen coordinate (regular node / hidden member / group socket literal). Collapsed-group renders as 240×(28+SOCKET_STEP·max(ins,outs)) bordered node at the topleft of the original member bbox; chevron ▾ expands, expanded chevron ▸ collapses; promoted sockets render on left/right with WIRE-palette colours + hover-tooltip showing `memberId · portName · portType`. Header drag still multi-drags via existing `onGroupDragStart`. New keybind `Ctrl+Shift+G` toggles collapse on the focused-node's containing group. Members hide in the per-node renderer when their group is collapsed. **9 tests** cover boundary detection (no-wire/internal/external), socket-id indexing, expand-idempotency, wire-rewrite correctness, and end-to-end cook equivalence (collapsed graph cooks to the same value as expanded). 1673 full suite green. (eng)
    - [x] **SLICE C3 ✓ SHIPPED 2026-05-21** — Group nesting. Design AgDR-0006. `LM_GRAPH.groups[].childGroupIds: string[]` field added (default `[]` — back-compat with legacy flat groups). Python: `expand_group_members(group_id, all_groups)` recursively walks the child-group tree returning every descendant leaf node id (cycle-safe via visited-set + 16-level depth cap) · `would_create_cycle(parent_id, candidate_child_id, all_groups)` walks the candidate's subtree to refuse loop-closing mutations · `_promoted_ports_for` takes the recursive member-set when `childGroupIds` is non-empty (boundary detection promotes ports whose counter-end is OUTSIDE the entire subtree; INTERNAL wires across any depth stay hidden). JSX mirrors `expandedMembersJS()` + `wouldCreateCycleJS()` + recursive `promotedPortsForGroup()`. Collapse CASCADES: a collapsed parent hides every descendant node AND every descendant group (`hiddenGroupIds` set built by walking `childGroupIds`); the parent's collapsed-node renders with the FULL subtree's boundary-ports. Expanded-parent bbox includes recursive leaf-node bboxes + any collapsed child-group's anchor rect. Drag cascades: `onGroupDragStart` collects the recursive leaf node-set so multi-drag moves every descendant in lockstep. **13 tests** cover flat/one-level/two-level expansion · self-cycle / indirect-cycle safety · direct / indirect / deep-ancestor cycle refusal · recursive boundary-port promotion · 2-level cook-equivalence (collapsed-parent graph cooks to same value as fully-expanded) · `expand_collapsed_groups` passes `all_groups` correctly. 1686 full suite green. (eng)
    - [x] **SLICE D ✓ SHIPPED 2026-05-20** — Wire + type system. Design AgDR-0007. Drag-time validation was already in place (`precheckWire` + bridge `can_wire`); slice D ADDED: (1) WIRE colour palette expanded to ~30 engine `PortType` enum keys (lowercased) — `number` yellow, `boolean` green, `list` blue, `geometry`/`walls` orange, `element`/`selection`/`view` purple, `file`/`path`/`image`/`ifc`/`csv` cyan, `host`/`document`/`model` warn, `string`/`text`/`object`/`trace` grey, `any` dashed; (2) Fancy-wire shape encoding at render time — read the source node's `cooked.value` and pick `strokeWidth` from data shape: scalar 1.6 / list 2.8 / tree 3.6 (+ dashed for tree or `any`); (3) New `data.passthrough` engine executor in `io_data.py` (identity); (4) New `reroute` grammar primitive — 13th kind, ≤20 cap intact — backed by `data.passthrough`. Reroute renders as a 24×24 round dot (no title bar, no body) with two sockets at vertical centre carrying full data-* attributes so `findSnapTarget` picks them up like any other socket. `test_grammar_is_small` still ≤20 (13). `test_payload_is_serialisable_and_complete` exempts `reroute` like `skill` (zero-config wire-routing dot). 58 tests green. CDP-PROVEN live 2026-05-20: grammar count = 13 with `reroute → data.passthrough`, palette places a 24×24 dot with 2 sockets (in + out).
    - [x] **SLICE E ✓ SHIPPED 2026-05-20** — Annotation bodies. Design AgDR-0008. `WatchBody` dispatches on `config.as`: `list` (truncated `<ul>` with hover-tooltips for long items, +N more), `table` (auto-infers columns from array-of-objects OR array-of-arrays OR array-of-scalars; first 30 rows + footer), `json` (pretty-printed, 2000-char truncate), `image` (URL-filtered to `http(s):` + `.png/.jpg/.jpeg/.gif/.webp/.svg` or `data:image/`), `view`/`model` (JSON placeholder + "3D VIEWER — COMING SOON" muted header; real 3D in a future slice). Empty state: italic "no data yet — wire a node to me". `NoteBody` reads `n.config.text` (or first `text` param), renders an in-house markdown subset via a small React-element tree (NOT `dangerouslySetInnerHTML` — XSS-safe): `#/##/###` headers, `**bold**`, `_italic_`, `` `code` ``, `[txt](url)`, `![alt](url)`, `- ` bullets. URLs filtered to `http(s):` / `data:image/`. Double-click body → textarea; blur or Escape commits to the node's `text` param; round-trips through `saveCurrentGraph`. `note` primitive gains a default `text: "_Note — double-click to edit_"` row. 58 tests green. CDP-PROVEN live 2026-05-20: place Note + Watch → Note renders italic markdown via `<em>`; Watch shows empty state; set `cooked.value=['walls','doors','windows','rooms']` + `as='list'` → `<ul>` with 4 `<li>` items.
    - [x] **SLICE F ✓ SHIPPED 2026-05-20** — Add-node UX. Design AgDR-0009. ONE unified overlay (the upgraded `WirePromotePalette`) invoked from four entry points: double-click empty canvas, global `Tab` key, `lm-wire-promote` event from wire-drop, and the "+ add node" toolbar. Autofocus search input with footer prefix hint; arrow-key navigation; Enter picks highlighted result or parses prefix grammar first. **Prefix grammar**: `~text` → Note prefilled `text:text`; `=expr` → Constant `value:expr`; `"text"` / `'text'` → Constant `value:text`; `N<M<N` → Input `default:M`. `addNodeFromLibrary` `_grammar` honours `_prefillParams` by overwriting/appending param rows + config. **Predictive autocomplete** ranks by (1) +100 type-compat with `from.type`, (2) +recent-use boost (cap 20, from `localStorage.__archhub_recent_node_use`), (3) +50 query-prefix-match. Top 12 shown. Result set covers all 13 grammar primitives + 16 per-host connector specialisations + saved Skills. Wire-promote auto-wires source → first compatible input. 58 tests green. CDP-PROVEN live: dispatch `lm-wire-promote` (from:null) → overlay opens; type `~hello prefix test` → Enter → Note spawned with `text:"hello prefix test"`; type `"banana"` → Constant spawned with `value:"banana"`.
    - [x] **SLICE G ✓ SHIPPED 2026-05-20** — Skill-as-node hybrid (MVP). Design AgDR-0010. **Save dialog** opened from the node menu's "★ Save as Skill" — name + description + category + Mode pair (Shared / Private). Payload extended with `meta:{mode, description, category}`. **Python `save_as_skill`** reads `payload.meta`, strips it from the stored graph, writes it at the envelope level. **`_scan_canvas_skills`** + `load_skill` surface `meta` to callers (default mode=`private` for legacy files, backward-compatible). **Spawn (`lm-spawn-skill`)** branches on mode: SHARED → places ONE `skill` grammar node with `skill_id`, `skill_mode:'shared'`, `config:{skill_id}` — edits to the source `.archhub-skill.json` propagate on next run (engine's `subgraph.user` already keys off `config.skill_id`); PRIVATE → existing inline-expand semantics (each placement independent). **Subgraph extraction** at save time now walks downstream-reachable from the focused node (not just single-node). **Disentangle** action added to the node context menu — only visible on `skill` nodes with `skill_mode:'shared'`; loads the source skill, expands it inline at the wrapper's location, drops wrapper + its wires. 58 tests green. CDP-PROVEN live 2026-05-20: place Constant → save as Shared via `bridge.save_as_skill` with `meta.mode='shared'` → `load_skill` returns `meta:{mode:'shared',description:'CDP test',category:'test'}` → `lm-spawn-skill` places a `skill` wrapper node with `kind:'skill'`, `cat:'skill'`, `skill_id:'cdp-shared-test'`, `skill_mode:'shared'`.
    - [x] **SLICE G2 ✓ SHIPPED 2026-05-21** — Skill mode badge + Promote Private→Shared. `bridge.get_saved_skills` surfaces `mode`/`description`/`category` from saved-skill envelope; new `bridge.promote_skill_to_shared(skill_id)` slot re-saves with `meta.mode='shared'`. JSX skill row renders single-letter badge (P muted / S accent-orange) with tooltip + ↑ Promote action (Private-only). Same row chrome. (eng)
    - [x] **SLICE H ✓ SHIPPED 2026-05-21** — Typed INPUT category. Founder caught the category-name-as-node failure: "is it ok when i tell you to have input to create a node called input? this hypothetical fucking input what is it?". Replaced bare `input`+`constant` primitives with 6 typed nodes: Number / Text / Boolean / File / Color / Parameter — each maps to `data.constant` (or `input.parameter`) with typed defaults pre-set. Legacy `input`+`constant` primitives marked `hidden=True` (kept for engine resolution of saved graphs). Added `hidden` field to `Primitive` dataclass; `grammar_payload()` skips hidden entries. `docs/NODE_GRAMMAR.md` rewritten as typed-catalogue contract. CDP-PROVEN: `◇ INPUT · 6` section in palette with all 6 typed nodes. (eng)
    - [x] **SLICE I ✓ SHIPPED 2026-05-21** — Typed LOGIC / SHAPE / WATCH / TRIGGER / OUTPUT categories. Same pattern as H: split each category-named primitive into typed nodes. LOGIC: If/Else, For Each, Switch, Merge. SHAPE: Filter, Sort, Unique, Pluck, Count, Flatten, First, Last (all map to `transform.apply` with `op` pre-set; Filter keeps `filter.apply`). WATCH: Table, List, JSON, Image (all `watch.preview` with `as` pre-set). TRIGGER: Manual Run, Schedule, Webhook, File Watch (all `trigger.emit` with `on` pre-set). OUTPUT: Result, File Save (new `output.file` executor), Console (new `output.console`), Display (new `output.display`). All legacy primitives marked `hidden=True`. 21 new tests for OUTPUT executors. (eng)
    - [x] **SLICE J ✓ SHIPPED 2026-05-21** — MATH + TEXT categories. New `app/workflows/nodes/math_text.py` with two engines: `math.op` (22 ops: add/sub/mul/div/mod/pow · round/floor/ceil/abs/neg · eq/neq/gt/lt/gte/lte · and/or/xor/not — string-coerce, div-by-zero→NaN, typed errors) + `text.op` (9 ops: concat/split/replace/format/match/upper/lower/trim/length). Typed MATH primitives: Add, Subtract, Multiply, Divide, Modulo, Round, Equal, Greater Than, Less Than, And, Or, Not. Typed TEXT primitives: Concat, Split, Replace, Format, Match. 65 new tests. (eng)
    - [x] **M1.a ✓ SHIPPED 2026-05-21** — Speckle wire substrate. `app/speckle_wire.py` `SpeckleWire(project_dir)` class wrapping `Operations.send/receive` with per-project `SQLiteTransport` at `<project>/Objects.db`. JSON-wrap coerce: `dict / list / scalar / None / bool` ↔ Speckle `Base`. Foreign Base passthrough (host-extracted Wall / Mesh / Room — `send_base` / `receive_base`). `use_default_cache=False` + `local_transport=self.transport` on receive — isolated from process-wide `%APPDATA%/Speckle/Objects.db` cache. specklepy 3.2.6 added as dep. **25 tests.** (eng)
    - [x] **M1.5 ✓ SHIPPED 2026-05-21** — Speckle Server lifecycle + 3 SHARE typed nodes. `app/speckle_server.py` lifecycle: `docker_available` / `is_running` / `start_local` (Docker Compose up + health-poll up to 90s) / `stop_local` / `status` + bundled `docker-resources/speckle-compose.yml` template (postgres + redis + minio + speckle-server). Typed nodes: `share.server` (lazy-start Docker stack, output server_url) · `share.publish` (DiskTransport always + optional ServerTransport push, output model_url) · `share.subscribe` (parse `speckle://local/<hash>` OR `http(s)://<server>/streams/<id>/objects/<hash>` OR bare hash, output value). Per founder decision 2026-05-21: DiskTransport default · server OPT-IN via these nodes. **36 tests.** (eng)
    - [x] **ADAPTER category ✓ SHIPPED 2026-05-21** — Cross-host native-type mapping. `app/workflows/nodes/adapter.py` 3 engines + 3 grammar primitives: `adapter.cad_to_revit_wall` (Polyline → Wall with level / wall_type / height / top_offset / structural — the AgDR-0012 litmus scenario) · `adapter.to_revit_directshape` (generic Base → DirectShape fallback under any built-in category) · `adapter.max_to_revit_family` (founder's 3ds Max mass → Revit FamilyInstance with category / family_name / family_template / parameter map). Annotates wire with `revit_*` metadata; Speckle Revit receive-side reads annotations to drive native creation. **18 tests.** (eng)
    - [x] **Router LIBRARY-FIRST gate WIRED ✓ 2026-05-21** — `app/llm_router.py:1257-1280` constructs `LibraryGate` + `TurnState` per LLM completion; `:1387-1424` inserts `gate.check()` BEFORE every `ToolEngine.invoke()` call inside the tool-call loop. Gate denial → synthetic tool_result with `status:error` + `retry_hint` + `code:"library_first_blocked"` + `continue` skips ToolEngine. LLM corrects in next iteration without aborting turn. Graceful degrade if gate module import fails (treated as advisory; turn continues). **7 tests** verify gate behavior + source-shape guards. (eng)
    - [x] **SLICE K ✓ SHIPPED 2026-05-21** — Polish + e2e proof. Collapsible palette sections (chevron ▾/▸ toggle; default collapsed). Typed-value indicators in GrammarBody: Color → 10×10 hex swatch · Boolean → ✓/✕ glyph. Internal type-identity params (`value_type`/`op`/`as`/`on`) hidden from canvas body (kept in inspector for debug). Boolean param-type `bool`→`boolean` (6 primitives — Boolean / If / ForEach / Merge / FileSave / Sort — now render toggle widget in inspector). `_lm_uid()` helper replaces per-ms-colliding `Date.now().toString(36).slice(-4)` at 6 callsites. **20 e2e integration tests** prove every typed primitive cooks through the real `WorkflowRunner` end-to-end (Number/Text/Boolean/Color/File, Add/Multiply/Equal/Not, Concat/Match, Result/Console/File Save, If/Merge, Filter/Count, Table, Manual Run). 1578 full suite green. CDP-PROVEN palette: `◇ INPUT·6 · ◇ CONNECTOR·16 · ◇ AI·1 · ◇ LOGIC·4 · ◇ MATH·12 · ◇ TEXT·5 · ◇ SHAPE·8 · ◇ OUTPUT·4 · ◇ SKILL·1 · ◇ WATCH·4 · ◇ TRIGGER·4 · ◇ NOTE·2` = **12 categories, 67 typed nodes**, all category-named-as-node failures closed. (eng)
    - [x] **AgDR-0019 Typed AI nodes ✓ SHIPPED 2026-05-21** — `ai` master split into 4 typed primitives matching the Slice H/I pattern. `ai_chat` → `conversation.chat` (chat UI rail), `ai_complete` → `llm.complete` (model + prompt params), `ai_classify` → `llm.classify` (model + options params), `ai_tools` → `llm.complete_with_tools` (model + prompt + allowed_tools params). Legacy `ai` master flipped to `hidden=True` so saved graphs with `kind:'ai'` + `action:<X>` still resolve to the right engine type. JSX `addNodeFromLibrary._grammar` branch now seeds `messages: []` ONLY for `ai_chat` placements — non-chat AI typed nodes render the generic param rail (the right surface for their action-specific config). Closes the slice K "ai action-specific rail (non-chat actions)" loose end. Grammar: 70 → 74 PRIMITIVES (under ≤75 cap); visible payload: 61 → 64 (under ≤70 bridge cap). **17 new tests** cover typed-primitive registration, master-is-hidden invariant, engine-type resolution for each typed node, legacy master back-compat (action selector still drives dispatch for saved graphs), param-surface per typed node, grammar-count cap, fixed-type (no-selector) shape. Updated `test_node_grammar.test_payload_is_serialisable_and_complete` + `test_new_bridge_slots.test_get_node_grammar_returns_the_canonical_grammar` invariants to reference `ai_chat` (the new family anchor) instead of `ai`. 1792 full suite green. (eng)
    - [x] **SLICE L ✓ SHIPPED 2026-05-21** (engine foundation; JSX flatten action follow-up) — Design AgDR-0020. New module `app/workflows/nodes/code.py` with two executors: `code.expression` (one-line `eval`) + `code.python` (multi-line `exec` body where `result = ...` is the output). Both take 3 wired inputs (`a`/`b`/`c`) + the `inputs` dict in scope. Sandbox is restrictive-by-default (`safe_mode=True`): two-layer — source-token pre-check rejects `__import__` / `__builtins__` / `__class__` / `open(` / `exec(` / `eval(` / `compile(` / `subprocess` / `socket` / `import ` / `from os` / `from sys`; restricted `__builtins__` dict carries only safe names (abs / min / max / sum / len / range / map / filter / sorted / int / float / str / bool / list / dict / tuple / set / etc.). Power users opt out via `safe_mode=False`. New typed grammar primitive `code` with `mode` selector (expression / python). Grammar: 74 → 75 PRIMITIVES (at ≤75 cap); visible payload: 64 → 65 (under ≤70 bridge cap). **24 new tests** cover arithmetic / builtins / str-concat / empty-source error / bad-syntax error / `result` semantics / no-result→None / multi-line body / inputs dict / sandbox-rejects (import / open / exec / dunder-class / import-keyword) / safe-mode-off-allows-import / safe-mode-off-still-surfaces-runtime-errors / grammar registration + mode-selector resolution / grammar-count cap / runner integration (both executors cook end-to-end). 1817 full suite green. Next L slice: JSX "Flatten chain to code" action that compresses a selected transform chain into one `code` node. (eng)
    - [x] **SLICE L flatten-to-code utility ✓ SHIPPED 2026-05-21** (pure-Python; JSX action wires up next tick) — New module `app/workflows/flatten_to_code.py` exposing `chain_to_expression(graph, node_ids)` (pure: returns `{expression, external_inputs, tail_id}` or `{error}`) + `flatten_chain(graph, node_ids)` (rewrites the graph, replacing the chain with one `code.expression` node + re-wiring externals → code-node inputs allocated as symbols `a`/`b`/`c`/.../`v8+`). Op→Python templates cover all 21 math ops (binary numeric / unary numeric / compare / boolean logic) + 5 text ops (concat / upper / lower / trim / length); `data.constant` collapses to its literal value; `data.passthrough` traverses through; multi-hop / nested operators wrap with parentheses for correct precedence; cycle detection raises typed error; multiple-tails / non-flattenable types / unknown ops all surface honestly. Wires rewriting: internal-to-chain wires dropped, external upstreams retargeted to the new code node's allocated symbol, downstream wires from the chain tail retargeted to the new code node's `value` output (intermediate-output downstream wires are honestly lost — documented). **15 new tests** cover: trivial constant-only chain, single-op two-externals, all-selected literal collapse, multi-step `(a + b) * 2`, text concat+upper, wire rewriting (externals + downstream + internal drop), engine cook-equivalence (`flatten_chain` output cooks to the same value as the original via `WorkflowRunner`) for both math + text, error paths (empty / unknown node / unflattenable type / unknown op / multiple tails), public API contract (`FLATTENABLE_TYPES` set). 1832 full suite green. Next: bridge slot `flatten_chain_to_code` + JSX context-menu action on multi-selection. (eng)
    - [x] **SLICE L bridge + JSX action ✓ SHIPPED 2026-05-21** — `bridge.flatten_chain_to_code(graph_json, node_ids_json)` exposes the pure util to the canvas via QWebChannel. JSX `NodeMenu` gains a "Flatten N nodes to Code" entry that appears when 2+ flattenable-kind nodes are selected (math/text ops + data.constant/passthrough); on click the slot rewrites LM_GRAPH, replaces selection with the new code-node id, and toasts the resulting expression. Robust error surface: bad JSON / non-object graph / non-array ids / unflattenable type / underlying util errors all return `{error: ...}` instead of throwing. **7 new bridge tests** cover happy-path (graph rewrite + new node + expression contains operator), empty-selection error, bad-JSON error, non-list ids error, non-object graph error, unflattenable type surfaces error from util, runner cook-equivalence through bridge (`(8*2)+3 == 19` cooks the same before + after flatten through the bridge slot). Babel-parse on the JSX clean. 1839 full suite green. (eng)
    - [x] **AgDR-0015 Phase 4 a11y baseline NUCLEUS ✓ SHIPPED 2026-05-21** (full sweep still pending — this is the nucleus floor) — Per AgDR-0015 §"Phase 4 — Accessibility sweep" the pre-slice score was 0/9. This slice promotes 13 icon-only `<button title="…">` to `<button title="…" aria-label="…">` (screen readers can now introspect them), AND injects a global `lm-a11y-styles` sheet: (a) `:focus-visible` outline 2px solid LM.accent + 2px offset on buttons / links / inputs / textareas / selects / `[tabindex]` so keyboard users see a clear ring on Tab focus (no ring on mouse focus — the difference matters for visual noise); (b) `@media (prefers-reduced-motion: reduce)` blanket-collapses animation + transition durations to 0.001ms for vestibular-sensitive users. Bulk-patch runs via in-place Python regex that only touches lines containing both `<button` AND `title=` AND no existing `aria-label=`; guard test enforces "every titled button has aria-label" so future devs can't regress. **5 new tests** pin: aria-label floor count ≥10, `:focus-visible` CSS present, `prefers-reduced-motion` handler present, every-titled-button-has-aria-label invariant, IIFE injection runs at module load. JSX Babel-parse clean. 1844 full suite green. Remaining Phase 4 work: ARIA roles on modals + focus traps + dropdown keyboard nav + image alt text + colour-blind-safe wire palette (out of nucleus). (eng)
    - [x] **AgDR-0015 Phase 4 modal a11y ✓ SHIPPED 2026-05-21** — Shared `_useModalA11y(onCancel)` hook + ARIA role + focus trap on every visible Phase-4 modal. Hook returns a ref; modal's inner panel attaches it. On mount: focuses the first focusable child. On Tab key: cycles within the modal (Shift-Tab wraps backwards). On Escape: calls `onCancel` (AINodeModal uses a guarded close that no-ops while `phase==='working'` so the LLM mid-call can't be aborted by an accidental Esc). Each modal stamped with `role="dialog"`, `aria-modal="true"`, and `aria-labelledby="lm-<modal>-title"` referencing a stable id on the heading element. Wired to 4 modals: GroupDialog, SaveSkillDialog, CreateNodeModal, AINodeModal. **8 new tests** pin: hook is defined with Tab + Escape branches, each of the 4 modals has matching `aria-labelledby` ↔ heading id pair, role+aria-modal floor count ≥4, every `role="dialog"` carries `aria-modal="true"` in a 4-line window (guards JSX line-split), `_useModalA11y` called ≥4× (one per modal), `ref={modalRef}` attached to panel. JSX Babel-parse clean. 1852 full suite green. Remaining Phase 4: dropdown keyboard nav + image alt text + colour-blind-safe wire palette. (eng)
    - [x] **AgDR-0015 Phase 4 dropdown keyboard nav ✓ SHIPPED 2026-05-21** — Custom pill pickers (button-arrays, NOT native `<select>`) now follow the WAI-ARIA Practices "radio group" pattern: container `role="radiogroup"` + `aria-labelledby` → group label; each pill `role="radio"` + `aria-checked={active}` + `aria-label` carrying the option's meaning; roving-tabindex (`tabIndex={active ? 0 : -1}`) so Tab lands once on the active pill + arrow keys move within the group. Arrow handler covers ArrowLeft/ArrowRight (horizontal pills) + ArrowUp/ArrowDown (mirror) + Home/End (jump to first/last); selection commits on landing + focus moves to the new pill so screen readers announce. Wired to 2 pill groups: GroupDialog STYLE picker (6 styles) + SaveSkillDialog MODE picker (Shared/Private). Image alt text + colour-blind-safe wire palette already satisfied (`<img>` tags both carry alt; Slice D wire-shape encoding gives non-colour affordance via dashed dasharray for tree/any types per AgDR-0007). **6 new tests** pin: both radiogroups present with labelledby anchors, radiogroup floor count ≥2 + radio floor count ≥4, `aria-checked={active}` present ≥2×, arrow handler covers all 6 keys (ArrowLeft/Right/Up/Down + Home/End), roving-tabindex pattern used. JSX Babel-parse clean. 1858 full suite green. Phase 4 NUCLEUS + modal + dropdown done. Remaining: WCAG 1.4.3 contrast audit (separate slice). (eng)
    - [x] **AgDR-0015 Phase 4 WCAG 1.4.3 contrast audit ✓ SHIPPED 2026-05-21** — New module `app/contrast_audit.py` with pure-Python WCAG primitives (`relative_luminance` via gamma-corrected sRGB → linear-light per WCAG spec; `contrast_ratio` symmetric across pair order; `passes_aa(body=4.5, large=3.0)` + `passes_aaa(body=7, large=4.5)`) + LM-palette extractor that pulls the live tokens from `studio-lm.jsx` (regex over the `const LM = { ... }` top-block; brace-depth-aware so nested object scopes don't leak in; indent filter ≤2 spaces keeps only top-level palette entries). Canonical-pair audit covers 10 pairs spanning primary text (ink on bg/bgPanel — AA body 4.5:1), secondary text (inkSoft on bg/bgPanel — AA body), accent CTA (AA large 3:1), status colours (ok/warn/err on bg — AA large), advisory (inkMuted — informational only). **23 new tests** cover math primitives (luminance 0/1 endpoints, mid-grey ≈ 0.216, B/W ratio = 21, symmetry, identity = 1, AA passes B/W, AA fails mid-grey/white, AAA stricter, shorthand `#abc`, bad-hex raises), palette extraction (expected keys, all hex format, specific value snapshots), per-pair audit (row count, primary AA body passes, secondary AA body passes, accent AA large passes, status colours all pass, advisory always passes regardless of ratio), regression guards (no-aa-body-fails + no-aa-large-fails so any future palette drift below threshold fails CI). 1881 full suite green. Phase 4 substantially complete. (eng)
  - Loose ends to sweep alongside: `ai` action-specific rail (non-chat actions); `trigger` executor; `switch` logic op; orphaned `NodePanel` helper state (pin / hidden / sort / `usage`) the deleted palette left behind. NB `LM_NODE_TEMPLATES` is deliberately KEPT — load-bearing I/O-shape infra for composer node-spawns + node duplication, NOT the old catalogue; `LM_LIBRARY` / `PIPELINE` are gone, `NODE_LIBRARY_v2.md` superseded.

  - **ARCHITECTURE LOCK 2026-05-20 — Direction X (AgDR-0012)** — slices A–G survive as canvas + bodies + rails + engine, but the WIRE LAYER migrates to Speckle and the IDE shifts to Composer-as-primary + canvas-as-result. Prototypes in `docs/prototypes/` reviewed; founder committed.
    - [ ] **M1 — Speckle as local wire (1–2 wks).** Embed `specklepy` (Apache-2.0). `SpeckleWire` class wrapping `Operations.send/receive` with `DiskTransport` at `.speckle/<project>/`. Replace `WorkflowRunner` wire serialization. PARALLEL: M1.a ReactFlow scaffold (one primitive ported). (eng)
    - [ ] **M2 — Revit Speckle connector (3–5 wks).** Bundle / IPC the Speckle Revit connector. `revit.send_to_speckle` + `revit.receive_from_speckle`. `@speckle/viewer` in inspector. **First valuable shippable when combined with M1 = 5 wks total.** (eng)
      - [x] **M2-Python ✓ SHIPPED 2026-05-21** — IPC-only path. Design AgDR-0017. New module `app/connectors/revit_speckle_ops.py` with 3 functions: `send_to_speckle(value, model_name, server_push, server_url)` wraps upstream via SpeckleWire + optional `push_to_server` fallback (failures don't block local write — `mode:"disk_only_after_server_fail"` is honest); `build_create_script(items)` is the PURE annotation→C# generator — classifies each item by `revit_*` annotation (`Walls`→`Wall.Create`, `revit_family_name`→`NewFamilyInstance`, `revit_directshape_category`→`DirectShape.CreateElement`, otherwise skip), wraps every item in try/catch so partial creates land cleanly, escapes C# string literals, converts mm→Revit-feet, applies `revit_parameters` via `LookupParameter().Set()`; `receive_from_speckle(source_url, instance)` parses the URL → pulls via SpeckleWire → emits one transactional C# body → POSTs to the existing RevitMCP `/exec` route; broker-offline check returns the standard `not_running` typed error (no fake create counts). Two new `ConnectorOp`s wired in `RevitConnector.build_ops()` — `revit.send_to_speckle` (kind=read since it doesn't mutate Revit; ships data outward through SpeckleWire) + `revit.receive_from_speckle` (kind=action, destructive=True — creates native elements). Closes the AgDR-0016 litmus chain Max-mass → Revit-family WITHOUT bundling the official Speckle Revit add-in (deferred as M2-Bundle, blocked on the Roslyn 4.11/3.4 AppDomain conflict in the founder-shared screenshot 2026-05-20). **21 tests** cover annotation classification (Walls/family/directshape precedence + skip), C# generation (Wall.Create / DirectShape / NewFamilyInstance with parameter passes, per-item try/catch, skipped-index tracking, empty-items handling, quote escaping), send-side shape preservation (dict/list/scalar/None), receive-side error paths (missing URL, unknown hash, remote URL not yet supported), end-to-end send→classify round-trip. Updated `tests/test_broker_connectors.py` invariants: revit op set now 15 (+ 2), destructive set now 4 (+ receive). 1707 full suite green. (eng)
      - [ ] **M2-Bundle — official Speckle Revit add-in** (deferred). Blocked on Roslyn 4.11 (RevitMCP) / 3.4 (pyRevit / Speckle) AppDomain conflict — separate .NET repo work. Required if/when the IPC C# path hits a feature ceiling (analytical model, sheets w/ viewports, complex geometry conversion). For 80% of cross-host use cases the IPC path is enough. (eng)
    - [ ] **M3 — Composer v1 + Library tools (6–8 wks).** Qt panel left. **Model-agnostic tool-use loop** (Anthropic / OpenAI / Gemini / Ollama / LM Studio). Tools: `library.search`/`list`/`create_node_type` (search-first), `graph.create_node`/`connect`/`set_param`/`run`, `speckle.send`/`receive`, `node.inspect`, `skill.save_as`. Plan / Auto / YOLO modes. Per-project `ARCHHUB.md` persistent memory. **LIBRARY-FIRST + USER-AGENCY mandates enforced via the 4-layer model in AgDR-0013** (system prompt + per-provider strict-tools where supported + router-level pre-execute gate + Pydantic NodeSpec validator). (eng)
      - [x] #P1 **AgDR-0038 slice 1 — Composer Capability Nodes** — `impl` discriminator in `app/workflows/custom_nodes.py` (`python`/`connector`/`ai`/`passthrough`) + restricted-builtins sandbox + bare-`code` back-compat + grounding test. Lets the Composer mint node types as data, not hand-coded per node. Design: `docs/agdr/AgDR-0038-composer-capability-nodes.md` · handoff: `docs/handoff/2026-05-22-composer-capability-nodes.md` (eng)
      - [x] #P1 **AgDR-0038 slice 2** — `impl.kind=connector` (typed wrapper over a host op) + `impl.kind=ai` (LLM-backed) capability executors (eng)
      - [x] #P2 **AgDR-0038 slice 3** — Composer tools in `app/tool_engine.py`: `node.search` / `node.create` / `node.place` / `graph.wire` (eng)
      - [x] #P2 **AgDR-0038 slice 4** — search → reuse → library auto-promotion loop; LIBRARY-FIRST enforced on minted nodes (eng)
    - [ ] **M4 — `ai.plan` as canvas node (9–10 wks).** Persists Composer turn + tool-call history. Plan-mode preview. Deterministic re-run (temperature=0 + cache). (eng)
      - [x] **M4 foundation ✓ SHIPPED 2026-05-21** — Design AgDR-0021. Engine + persistence + grammar foundation; Composer panel JSX integration ships next tick. New module `app/plan_history.py` (`PlanHistory(project_dir)` rooted at `<project_dir>/.archhub/plans/<plan_id>.json`; atomic save via tempfile + os.replace; deterministic `PlanHistory.id_for(prompt, model, extra)` returns 16-hex sha256 prefix; corruption-resilient `load` returns None on bad JSON; `list_ids` ordered most-recent-first by mtime; `prune(keep_last)` for future cleanup). New executor `app/workflows/nodes/ai_plan.py` (`ai.plan`) wraps the registered `llm.complete_with_tools` engine + persists each cook's plan record (success OR failure both saved — audit trail must include failures). Replay mode (`config.replay=True`) returns the cached record without re-calling the LLM when input hash matches; cache miss + replay falls through to a fresh LLM call (graceful, never errors). `allowed_tools` config accepts comma-separated string OR list; threaded through to the wrapped executor so the LIBRARY-FIRST gate (AgDR-0013) still fires. New typed grammar primitive `ai_plan` (5th typed AI node after AgDR-0019). Grammar cap raised 75 → 80 in `test_node_grammar.test_grammar_is_small_a_grammar_not_a_catalogue` (75 was the post-SLICE-L peak; +1 for `ai_plan` is honest growth, still under the legacy 80-node sprawl). **29 new tests** cover: id determinism (same → same, different prompt/model/extra → different, 16-hex format), persistence (save/load round-trip, archhub/plans subdir creation, rejects-without-plan_id, non-dict rejection, list ids, delete, prune keep_last + no-op-under-cap, list-records-most-recent-first, corruption returns None), grammar (primitive registered + resolves + in payload + carries replay param + count under 80 cap), engine (fresh-run calls LLM + persists, persists to disk, replay returns cache without LLM call, replay-falls-through-on-miss, persists failure record, allowed_tools threading via comma-string parse). Updated `test_typed_ai_nodes.test_grammar_count_after_ai_split` from `ai == 4` to `ai == 5` (AgDR-0021 adds the 5th typed AI primitive). 1911 full suite green. Next M4 slice: bridge slot to surface `PlanHistory.list_records` + JSX Composer panel showing plan history with replay button. (eng)
      - [x] **M4 bridge slots ✓ SHIPPED 2026-05-21** — Three new bridge slots expose `PlanHistory` to the canvas via QWebChannel: `bridge.get_plan_history(project_dir, limit)` returns `{records:[...], count, project_dir}` (most-recent first, limit clamped to ≥1); `bridge.get_plan_record(plan_id, project_dir)` returns the full record or `{error: "not_found"}`; `bridge.delete_plan_record(plan_id, project_dir)` returns `{ok: bool}`. Empty `project_dir` falls back to `speckle_wire.default_project_dir()` (canonical `%LOCALAPPDATA%/ArchHub/projects/default`). Each slot wraps the bridge contract — return JSON string; typed error envelope on exception. **9 new tests** cover: list returns records sorted most-recent-first, limit respected, empty-dir → empty list, default project_dir fallback when project_dir is "", limit=0 clamped to ≥1, get-by-id returns record (record's own status/error fields distinct from bridge-level `not_found` sentinel), missing id → `not_found`, delete removes file + cascades into list output, delete missing returns `{ok: false}`. 1920 full suite green. Next M4 slice: JSX Composer panel renders the history list + replay button. (eng)
    - [x] **Code typed-split ✓ SHIPPED 2026-05-21 + CDP-VERIFIED** — Founder caught the last dropdown UX violation: `code` primitive carried a `mode: expression/python` selector → right panel showed the dropdown. Applied Slice-I typed-per-action pattern: `code_expr` → `code.expression` (params: `expr` + `safe_mode`); `code_py` → `code.python` (params: `body` + `safe_mode`). Legacy `code` master flipped to `hidden=True` so saved graphs still cook. Grammar audit confirms ZERO visible primitives still expose selector dropdowns. JSX `flatten-to-code` recogniser updated to block both `code_expr` + `code_py` as flatten sources (already-flattened nodes can't re-flatten). Updated tests pin: legacy master hidden, both typed primitives in payload, each typed primitive carries ONLY relevant params (no `mode`/`expr` cross-leak). Test caps updated 75 → 80 in `test_adapter_nodes` + `test_typed_ai_nodes` to match `test_node_grammar` (the cap was raised post-AgDR-0021 but stale references lingered). **CDP-VERIFIED on the live app**: `bridge.get_node_grammar()` returns 67 visible primitives, `masters_with_selector: []` (zero), `code` master hidden + only `code_expr`/`code_py` surfaced, `ai` master hidden + only `ai_chat`/`ai_complete`/`ai_classify`/`ai_tools`/`ai_plan` surfaced. 1944 full-suite green. (eng)
    - [x] **AgDR-0023 RevitMCP Roslyn isolation Python-side ✓ SHIPPED 2026-05-21** — Founder gripe 2026-05-21 "WE SHOULDN'T CONFLICT WITH OTHER ADDINS" — fix the CLASS, not the symptom. AgDR-0023 locks architectural fix: RevitMCP NEVER loads `Microsoft.CodeAnalysis` (Roslyn) into the Revit AppDomain; instead spawns `csc.exe` as a subprocess to compile each /exec script, then `Assembly.LoadFile`s the result. Roslyn lives in its own process → ArchHub coexists with pyRevit + Speckle + any future Roslyn-using add-in forever. .NET-side `RoslynIsolator.cs` ships in the separate RevitMCP repo. THIS PR ships Python-side support: (a) `revit_broker._warn_legacy_compiler_once(port, compiler)` fires one-time deprecation log per port when /ping returns `compiler:'in_process_roslyn'`; silent for `subprocess_csc` and for absent field (back-compat with pre-AgDR-0023 builds); (b) `revit_connector._exec` translates `error_code:'csc_missing'` from /exec into a typed OpResult pointing the user at `docs/RUN-REVIT.md` for Build Tools install; non-csc errors pass through unchanged; (c) `docs/RUN-REVIT.md` (new) walks the founder through the one-time Build Tools install + verification (`curl /ping` shows `compiler:subprocess_csc`) + rejects "disable Speckle / pyRevit" as long-term answer. **8 new tests** cover: one-time warn on legacy, silent on modern, silent on absent, per-port distinct warn, csc_missing → typed OpResult with build-tools pointer, other errors pass through, AgDR doc exists, RUN-REVIT.md exists with required anchors. 1968 full-suite green. Next: hand AgDR-0023 §"What ships in the SEPARATE RevitMCP .NET repo" to the .NET maintainer for the structural fix. (eng)
    - [x] **AgDR-0022 + ReactFlow P2.a groundwork ✓ SHIPPED 2026-05-21** — Design AgDR-0022 locks the migration strategy: feature-flagged coexistence (`localStorage.archhub.canvas`), `LM_GRAPH`-as-source persistence (RF never owns storage), LM-token-first styling, 4 sub-slices (P2.a substrate · P2.b primitives · P2.c edges + connector · P2.d groups). P2.a groundwork ships: module-level `_readCanvasFlavor()` + `_setCanvasFlavor(name)` (memoised reader; dispatches `archhub-canvas-flavor` event so re-render is instant — no app restart); window globals `__archhubCanvasFlavor` / `__archhubSetCanvasFlavor` for CDP audits + Settings panel; default 'custom' so today's UX is untouched. New stub `NodeCanvasRF_Stub` component renders the preview placeholder when the flag is `reactflow` — references AgDR-0022 in the user-facing copy + carries a "Back to custom canvas" button so flip is reversible in one click. Stub passes the LM-token-first guard (zero hex literals except whitelisted `#fff` for accent-on-orange button text). **9 new tests** pin: reader + writer defined, default-custom invariant, event-dispatch on flip, window-globals attached, stub component exists with `data-testid`, AgDR reference in copy, no-hex-literals guard, back-button present, AgDR-0022 doc exists with all 4 sub-slice anchors. JSX Babel-parse clean. 1960 full-suite green. Next: P2.b sub-slice — actually load `@xyflow/react` + render typed primitives as RF nodes. (eng)
    - [x] **CDP grammar-audit tool ✓ SHIPPED 2026-05-21** — Founder can re-run any time to verify dropdown gripe stays closed. `tools/cdp_grammar_audit.py` connects to ArchHub's CDP on port 9223, pulls `bridge.get_node_grammar()` via async resolution + audits the live DOM for `<select>` elements, classifies failures across 4 invariants (visible-master-with-selector, param-renders-as-dropdown, `code`/`ai` master not hidden), exits 0 PASS / 1 FAIL. Smoke tests pin: module imports, `_cdp_url` returns None gracefully when ArchHub not running, docstring documents the launch command (`QTWEBENGINE_REMOTE_DEBUGGING=9223 pythonw app/main.py`), all 4 invariants present in source. Validates today against live ArchHub: 67 visible primitives + 0 dropdown masters + `code`/`ai` masters both hidden + 2 typed code nodes + 5 typed AI nodes surfaced. **5 new tests**. 1951 full-suite green. Founder can run `python tools/cdp_grammar_audit.py` after any future grammar change to confirm zero dropdown regressions live. (eng)
    - [x] **Grammar-health audit ✓ SHIPPED 2026-05-21** — QA infrastructure (no AgDR needed — pure inspection tooling). New `app/grammar_health.py` module sweeps the relationships between three sources of truth: `node_grammar.PRIMITIVES`, `workflows.registry._REGISTRY`, `connectors.revit_speckle_ops._classify_item`. Catches drift classes: (1) primitive resolves to UNREGISTERED engine type → cook would error with "no executor for X" (e.g. SLICE-2 connector regression class); (2) adapter writes annotations the receive-side classifier doesn't recognise → silent skip during receive (e.g. AgDR-0018 Batch 2 introduction class); (3) primitive without blurb or display → palette UX bug; (4) duplicate primitive kinds → ambiguous engine resolution. Per-adapter classifier-symmetry uses sentinel inputs declared in `_adapter_annotation_sentinel(adapter_kind)` — new adapter without a sentinel emits a warning so the dev adds one. `format_health_report(report)` renders human-readable output for CI failure messages + future `/audit grammar` command. **14 new tests** cover: report dataclass shape + `ok` property, today's grammar passes every critical check (pinned regression — any future drift fails CI), every visible primitive has blurb + display, no duplicate kinds, every engine type referenced by grammar is registered, every adapter's annotation is classifier-recognisable (not 'skip'), every adapter has a sentinel for the audit, format-report human-readability + CRITICAL section + WARNINGS section rendering, synthetic-regression catchers (missing executor + duplicate kind + missing blurb all flagged when injected). 1933/1934 full suite green (1 pre-existing flake `test_delete_session_removes_file` documented in NEXT-30-DAYS — passes isolated, fails in full-suite cross-file pollution; not introduced by this slice). (eng)
    - [ ] **M5 — Cross-host wire AutoCAD → Revit (11–12 wks).** Bundle Speckle AutoCAD connector. Wire validation, lossiness preview, DirectShape fallback explanation. (eng)
      - [x] **M5-Litmus ✓ SHIPPED 2026-05-21** — End-to-end Max-mass → Revit-family chain verified pure-Python, no live hosts. `tests/test_litmus_max_to_revit.py` (4 tests). Exercises: mock Max mass → `adapter.max_to_revit_family` enriches with `revit_*` annotations → `send_to_speckle` writes via SpeckleWire → SpeckleWire.receive round-trips preserving annotations → `_classify_item` returns "family" → `build_create_script` emits `NewFamilyInstance` C# with the right family + parameter map. Also covers: list-of-masses parallel (3 masses → 3 try-catch blocks with idx tags), unannotated-mass skip path (no fake creation), CAD-to-Wall parallel chain (Polyline → adapter.cad_to_revit_wall → `Wall.Create` C# with mm→ft conversion). Pins every link in the chain so a regression at any stage surfaces directly. 1711 full suite green. (eng)
      - [x] **push_to_server canonical entry-point ✓ SHIPPED 2026-05-21** — `app/speckle_server.push_to_server(value, server_url, model_name, token=None)` is the SINGLE canonical Speckle Server push function. Returns the canonical `<server>/streams/<model>/objects/<hash>` URL. Used by BOTH `share.publish` AND `revit.send_to_speckle` — duplicate inline server-push logic in `share.py` collapsed into the canonical call (per ENGINEERING MANDATE "fix the CLASS of bug": one implementation, no drift). 4 new tests cover canonical URL shape, exception propagation, trailing-slash normalisation, revit.send_to_speckle delegation. 1715 full suite green. (eng)
      - [x] **ADAPTER Batch 2 ✓ SHIPPED 2026-05-21** — Design AgDR-0018. 3 new typed adapter primitives + receive-side extensions + a new dedicated batch-update op. (1) `adapter.cad_to_revit_detail_line` annotates polylines for view-specific `DetailCurve.Create` (configurable view_id + line_style). (2) `adapter.rhino_to_revit_beam` annotates curves for native `FamilyInstance(StructuralType.Beam)` (configurable family + type + level). (3) `adapter.excel_to_revit_params` folds Excel rows into `{revit_element_id, revit_parameters}` dicts (configurable element-id column + ignore-columns list). `revit_speckle_ops.py` gains `_emit_beam` + `_emit_detail_line` C# generators (degenerate-polyline → clean error block; view_id=0 → ActiveView) + classifier extension (new kinds `beam`, `detail_line`, `parameter_set`). `revit.batch_set_parameters` is a NEW op (kind=action, destructive=True) that consumes excel-param items + emits one transactional C# that looks up each element by ElementId + pushes every parameter via `LookupParameter().Set()` (bool→0/1, numeric→bare value, string→escaped literal); per-element + per-parameter try/catch so partial work commits cleanly. Grammar count: 67 → 70 (under the ≤75 cap). Revit op set: 15 → 16. **22 new tests** cover adapter executors (annotations, list-input, defaults, ignore-columns, None filtering), classifier extensions, C# emission (Wall + Beam + DetailLine + FamilyInstance + DirectShape + parameter-set skip), batch-set generator (per-param `LookupParameter`, bool→int, skipped-items, missing-URL, remote-URL). 1743 full suite green. (eng)
      - [x] **Tool-engine integration for Revit↔Speckle ops ✓ SHIPPED 2026-05-21** — 9 tests in `tests/test_tool_engine_revit_speckle.py` verify the LLM-visible tool surface for the 3 new Revit↔Speckle ops. Pin: (a) tool name shape uses `__` instead of `.` (provider-compatible — Anthropic/OpenAI/etc reject `.` in tool names); (b) all 3 ops appear in `_connector_tool_specs()`; (c) destructive ops get a `[MUTATES THE HOST]` hint in their description (the LLM knows to confirm); (d) `invoke()` routes `revit__send_to_speckle` through the real connector + returns the connector's value; (e) `ask` policy returns `needs_confirmation` and is bypassable via `user_confirmed=True`; (f) `deny` policy is NOT bypassable even with `user_confirmed=True`; (g) `receive` + `batch_set` honestly surface broker-offline errors (no fake create_count); (h) input-schema declares every ParamSpec the op accepts (including `required: ["source_url"]` for the receive/batch ops). 1752 full suite green. (eng)
    - [ ] **M6 — Optional ServerTransport (13–14 wks).** Cloud / self-hosted Speckle Server as collaborator. "Publish this wire" creates remote Model. Speckle Automate webhooks for live cross-machine wires. (eng)
      - [x] **M6-AutoPublish ✓ SHIPPED 2026-05-21** — Per-graph `auto_publish` hook in `WorkflowRunner.run_all()`. Graph-level config `{enabled, server_url, model_name, project_dir}` lifts the requirement to wire `share.publish` explicitly: every successful sink ships through SpeckleWire automatically after the cook, with optional server push delegated to the canonical `speckle_server.push_to_server`. Honest reporting: each sink gets a per-entry outcome (`status: ok`/`skipped`/`error`, `mode: disk`/`server`/`disk_only_after_server_fail`, with `server_error` carrying the typed exception when the server push fails). Server-push failures never taint the cook — disk write always succeeds first. Disabled path is a strict no-op — runner does NOT instantiate SpeckleWire when `auto_publish.enabled=False` (verified by patching SpeckleWire to `_Boom` and asserting zero instantiations). **8 tests** cover default-off / explicitly-disabled / enabled-to-disk / multiple-sinks / skipped-on-no-value / server-push-failure-fallback / server-push-success-url / untouched-when-disabled. 1763 full suite green. Next M6 slice: Speckle Automate webhooks for live cross-machine wires. (eng)
      - [x] **M5 Cross-host send parity ✓ SHIPPED 2026-05-21** — `autocad.send_to_speckle` + `max.send_to_speckle` parallel the AgDR-0017 Revit pattern (kind=read, destructive=False — these ops ship upstream values through SpeckleWire and do NOT mutate the host). Both delegate to the canonical `revit_speckle_ops.send_to_speckle()` with `source_host='autocad'`/`source_host='max'` so the Speckle commit carries `archhub_source: <host>` for receive-side disambiguation. The old `revit_source: True` flag stays for back-compat (the existing receive-side classifier didn't read it; new cross-host receivers read `archhub_source`). AutoCAD ops: 9 → 10. Max ops: 7 → 8. **8 tests** cover op registration shape, end-to-end SpeckleWire write per host, archhub_source marker per host, back-compat Revit marker still present, LLM tool surface includes new `autocad__send_to_speckle` + `max__send_to_speckle`. 1771 full suite green. (eng)
    - [ ] CONTINUOUS: Library validation hardening (reject non-modular specs — **Layers 3 + 4 + library module + persistence + ToolEngine wiring + Bridge slots all shipped 2026-05-20**: `app/library_validator.py` (100 tests) + `app/library_gate.py` (38 tests — model-agnostic pre-execute gate with retry_hint + wire-format canonicalisation) + `app/library.py` (31 tests — register/search/list/inspect/delete with deterministic ranking + save_to_disk/load_from_disk) + `app/library_persistence.py` (16 tests — atomic JSON storage + corruption defense at `%LOCALAPPDATA%/ArchHub/library/registry.json`) + `app/library_seeds.py` (5 primitive specs validated + registered) + `app/tool_engine.py` integration (14 tests — 5 `library_*` tools for LLM via `_invoke_library_handler`) + `app/bridge.py` slots (20 tests — 5 `library_*` slots for JSX with bootstrap-on-first-call: load-from-disk-if-present else seed) per **AgDR-0013 enforcement architecture** + **AgDR-0014 design-system tokens**; integration into LLM router (M3 — insert `LibraryGate.check` at `llm_router._complete_once:1386`) + visual UI token migration per **AgDR-0015** (Phase 1 LM.size/font/radius/shadow/motion/brand tokens additive ✓ shipped; Phase 2 ReactFlow scaffold, Phase 3 sweep, Phase 4 a11y remain) ahead); skill dependency-graph warnings.

  - **WHAT COLLAPSES under Direction X:**
    - `geometry.cad_to_revit` adapter op (planned in AgDR-0007, never built) → replaced by `autocad.send_to_speckle` + Speckle wire + `revit.receive_from_speckle`.
    - `ArchHub.PortType` enum → replaced by Speckle `speckle_type`.
    - `glue.script` as cross-host adapter → demoted to fallback ("Speckle can't carry this"), kept in library under `Glue` category.
    - Custom canvas (NodeCanvas, sockets, drag, pan, zoom) → replaced by ReactFlow (`@xyflow/react`, MIT).

  - **WHAT'S REINFORCED:**
    - The LIBRARY is the user's living inventory. Search-first for the agent; browse-anytime for the user. `NodeLibrary` modal + `WirePromotePalette` STAY as user-facing surfaces.
    - Modularity validated at registration: typed I/O + `config_schema` + `description` + `examples`. The validator rejects non-modular specs.
    - User agency engineered, not honour-system: Plan/Auto/YOLO modes, undo via Speckle Versions, library browse, canvas direct manipulation.
- [ ] #P1 archhub.io go-live — DNS records, Fly deploy, Resend domain verification, `PUBLIC_URL` secret (ops)
- [x] #P1 `tool_policies` override store audited (2026-05-18) — root fix: `tests/conftest.py` now isolates `secrets_store` suite-wide (autouse fixture), so no test can pollute the real settings store ever; verified 1156 tests pass. The existing all-"allow" overrides were NOT test-created (no test bulk-sets "allow"; `test_ai_behaviour` was already isolated) — they are founder/app Settings state. Resetting them is the founder's call (Settings → AI Behaviour → Reset) (qa)
- [x] #P2 SessionCard host pills + last-message preview — `get_sessions` now emits `host` / `last` / `node_count` / `messages` via `session_io.list_sessions_rich`; the JSX card already had the render slots; shipped 2026-05-18 (eng)
- [x] #P0 Firm-tier quota revenue bug (2026-05-18) — a company on the Firm plan was capped at the 2000 `companies.msg_limit` column DEFAULT instead of its 1,000,000 quota (`config.PLAN_QUOTAS["firm"]`) — 99.8% of paid quota silently withheld. Root: `db.create_company` never seeded `msg_limit`, and `db.update_company` (the billing webhook's only company call) updated `plan` without re-deriving `msg_limit`. Audit found the class is wider — `billing.py` routed only `checkout.session.completed` to companies; `customer.subscription.updated` (Studio→Firm upgrade) and `.deleted` (cancellation) were user-only, so an upgrade/cancel never reached the company row. Root fix: the plan→msg_limit invariant now lives in `db.create_company` + `db.update_company` (a plan change re-derives `msg_limit` + resets `msg_used` — impossible to set a plan and leave a stale quota); `billing.py` gained company branches for subscription updated/deleted via `_company_from_subscription`; dead `update_company_quota` (aspirational, never wired) deleted. +8 tests; 180 cloud tests green (eng)
- [x] #P1 Skills panel "empty & not working" (2026-05-18) — the founder saw the Skills list show ~19 skills that did nothing on click. Root: a three-way store drift — `get_saved_skills` listed `skills.library` (engine-format Workflow store), `load_skill` globbed the source-tree `app/skills/`, and storage/export/import used `%LOCALAPPDATA%/ArchHub/skills/`. The panel listed skills the loader could never find → every click 404'd. The earlier `load_skill` slot (c93966a) was a patch — it un-deadened the button but read the wrong store. Root fix: `get_saved_skills` + `load_skill` now share one resolver (`bridge._scan_canvas_skills` — canvas-format: `%LOCALAPPDATA%/ArchHub/skills/` user store + `app/skills/` shipped seeds); `save_as_skill` writes the user store not the source tree; JSX `LM_SAVED_SKILLS` fallback is `[]` not 6 hardcoded demo skills. Guard test pins list⊆loadable. 54 bridge tests green (eng)
- [ ] #P2 Home filter chip `scheduled` is dead — no session-schedule model exists; remove the chip. (The `workflows` chip now works — `get_sessions` emits `node_count`.) (eng)

## NEXT 30 DAYS

- [ ] #P2 `/dashboard` authed-roster test — current tests only assert the page renders, not the authenticated company/team render (qa)
- [ ] #P2 First-run profile capture — zero automated coverage on the `get_profile` / `save_profile` bridge slots + `FirstRunProfile` (qa)
- [ ] #P1 Cloud Pro / Studio paid tiers — auth + Stripe phase per `docs/CLOUD_REVIVAL_PLAN.md` (eng)
- [ ] #P1 Session tokens are immortal — `db.issue_token` stores only `token`/`user_id`/`created_at` (no expiry) and `db.user_for_token` does ZERO expiry check, yet `auth.py` advertises a 90-day `expires_at` to the client. The advertised expiry is theater — a leaked `ah_live_…` token grants permanent access. Fix: store an expiry on the `tokens` row + enforce it in `user_for_token` (mirror the `codes`-table check at `db.py:586`). Security (eng)
- [ ] #P2 Company seat-limit ignores outstanding invites — `companies.py invite_member` (line 219) comments "count existing members + outstanding invites" but only counts members (`db.count_company_members`). A company over-invites past `seat_limit`: 3 members + N pending invites all pass the check, then all accept. Count members + un-accepted invites (eng)
- [ ] #P2 `proxy.chat_completions` has no success-path test — only the two 402 quota-exhausted cases are covered (`cloud_backend/tests/test_company_quota.py`). The happy path (in-quota request → model route → 200 + usage increment) has zero coverage; the core revenue path is untested (qa)
- [ ] #P2 Polar billing is company-blind — `polar.py` only ever calls `db.update_user_plan`; no webhook path (`subscription.created/updated/canceled`) routes to a company, and `create_checkout_url` hardcodes `archhub_kind:"user"`. A company on `BILLING_PROVIDER=polar` cannot get a paid plan at all. Stripe is the active provider so this is a gap not a regression; build company checkout + company webhook routing (mirror `billing._company_from_subscription`) before Polar goes live (eng)
- [ ] #P2 Surface engine-format library skills in the canvas Skills panel — the `skills.library` Workflow store (seed skills from `production_seeds.py`/`seeds.py`, and the chat skill-matcher's source) is engine-shaped (`type`/`config`/`inputs`); the canvas panel only renders canvas-format skills (`cat`/`x`/`y`/`ins`/`params`). Surfacing the library seeds in the panel needs an engine→canvas node converter — the inverse of the #P0 canvas-Run binding adapter; build alongside #P0 slice-1 (eng)
- [ ] #P2 Delete dead `SkillsPanel` + `SearchPanel` JSX components — `studio-lm.jsx:2686`/`:2761` define both but nothing renders them (`panel` state is `'nodes'`-only since the chats/skills/search panels were removed — `studio-lm.jsx:602`). ~140 lines of decorative dead code; the live Skills surface is the node-library `★ SKILLS` section (eng)
- [ ] #P2 Deploy the 3ds Max host add-in — `payload/max/` does not exist; the connector is code-complete but `probe()` honestly reports `missing` until the add-in ships. Build + deploy from `payload/sources/max_mcp/` (ops)
- [ ] #P2 Outlook connector op gap — it ships 8 named ops, but `ai_behaviour.py` + `outlook_runner` were built for ~15 (`set_categories`, `set_categories_by_filter`, `auto_categorize_by_*`, `search_threads`, `list_folders`, `move_to_folder`, `flag_for_followup`, ...). The `execute_python` escape hatch was wired 2026-05-18; wire the named category/folder ops too (eng)
- [ ] #P2 Connector escape-hatch parity — surface ops the runners already support but the connectors don't expose: `revit.execute_csharp` + `revit.screenshot`, `autocad.execute_csharp`, `max.execute_python`, `rhino.screenshot`, `{illustrator,indesign,photoshop}.execute_jsx`. Template: `outlook.execute_python` (commit ae79db5) (eng)
- [ ] #P2 Tool-registry unification — `tool_engine.TOOLS` (hand list) and `connectors.base` ops are two parallel registries, both emitted to the LLM; 6 hosts get duplicate tools. Audit 2026-05-18 confirmed the surface is functionally honest (no dead routes) but drifted. After escape-hatch parity migrates the unique `TOOLS` ops to connector ops: delete the ~42 stale `TOOLS` host entries, recouple `llm_router._filter_tools_by_relevance`, drop the legacy `HOSTS`/`_http` stack, then add a tool-surface drift guard (eng)
- [ ] #P2 Bridge dead-slot cleanup — ~21 `@pyqtSlot`s have no JSX caller (memory mutations, trigger arm/disarm, node-MCP dispatch, settings housekeeping: `set_theme`/`export_all`/`set_host_active`/`wire_transform`/...), and `index.html` prefetches 4 slots into window vars `studio-lm.jsx` never reads. For each: wire the intended UI action, or delete the slot (eng)
- [ ] #P2 `acad` vs `autocad` host-name mismatch — the AutoCAD connector's `host` and the `tool_engine`/`ai_behaviour` family name disagree. Pick one, repo-wide (eng)
- [x] #P2 `test_delete_session_removes_file` cross-file test pollution ✓ FIXED 2026-05-21 — Added autouse `_isolate_session_io_module_state` fixture to `tests/test_new_bridge_slots.py` that re-stamps `session_io.SESSIONS_DIR` via monkeypatch on EVERY test in the file. Structural isolation: any earlier test's leak is overridden before each `bridge_inst` slot invocation + auto-reverted after via monkeypatch teardown. Per ENGINEERING MANDATE (fix the CLASS of bug, not the symptom). Guard: new `tests/test_session_io_isolation.py` (5 tests) pins the fixture's existence + autouse-true contract + monkeypatch-usage so future devs can't accidentally remove it. 1939 full-suite green (verified twice consecutively, no flake). (qa)
- [ ] #P2 Flaky test `test_host_executor_returns_typed_envelope` — asserts Revit version `"2025"` but the host-node executor returns the machine-detected version (`"2020"` on this box). Pre-existing, environment-dependent. Fix: don't hardcode a machine-dependent version, or make the executor echo the requested config (qa)
- [ ] #P1 AgDR-0037 follow-ups (was "AgDR-0032 follow-ups" — renumbered 2026-05-22 after a duplicate-id collision) — guard tests for the three net8-host /exec bugs (refs builder, response file, ALC-aware load); mirror the refs-builder shape in `AcadMCPCore.cs` once that csproj exists; defense-in-depth `CoreLoader.Unload` GC-pass + weak-reference wait so stale collectible ALCs drop faster (eng + qa)
- [ ] #P2 Canonical `RevitMCPCore.dll` redeploy — PA-JPD17-04 is live on `RevitMCPCore-hotfix4.dll` (hot-loaded via `/reload` at AgDR-0032 ship time). Next clean Connectors-panel toggle should redeploy the canonical-named `RevitMCPCore.dll` into `%LOCALAPPDATA%\ArchHub\Revit\2025\` so future Revit launches load the canonical Core, not the hotfix sidekick. AgDR-0029's build-pair is the dispatcher — verify it copies the freshly-built Core over any stale hotfix sidekick (ops)

## LATER

- [ ] #P2 Civil 3D connector — blocked on Autodesk licence funding; design memo `docs/CIVIL_3D_ROADMAP.md` (rnd)
- [ ] #P2 SOC 2 Type I audit — triggered by first enterprise prospect with budget (ops)
- [ ] #P2 Canvas v2 power-user features — multi-select align, copy/paste subgraph, mini-map; spec `docs/CANVAS_PLAN.md` §7 (eng)
- [ ] #P2 Connector depth pass — bring all 18 host families to genuine parity; spec `docs/CONNECTOR_MASTER_PLAN_2026-05-15.md` (eng)
- [ ] #P2 Built-in starter skills — ship clean `app/skills/*.archhub-skill.json` templates so a fresh canvas has learnable examples; `test_shipped_canvas_skills_are_loadable` skips until they exist (eng)

## Done — last 7 days

<!-- autopopulated by agents/roadmap_dispatcher.py — do not edit by hand -->

## Design references

Not roadmaps — architecture / decision memos, kept for rationale. Anything
actionable from these is tracked as a backlog item in the sections above.

- `docs/CANVAS_PLAN.md` — visual-canvas architecture (NodeGraphQt history + current JSX)
- `docs/NODE_GRAMMAR.md` — node-system redesign: the node grammar, ~12 primitives, skill-as-node, build slices (supersedes `NODE_LIBRARY_v2.md`)
- `docs/CONNECTOR_MASTER_PLAN_2026-05-15.md` — 18-host connector build spec
- `docs/CLOUD_REVIVAL_PLAN.md` — cloud architecture decision (Cloudflare / Neon proposal)
- `docs/CIVIL_3D_ROADMAP.md` — Civil 3D connector design memo (deferred feature)
