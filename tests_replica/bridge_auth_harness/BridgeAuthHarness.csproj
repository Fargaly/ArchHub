<Project Sdk="Microsoft.NET.Sdk">
  <!-- Court fixture only: runs the SHIPPED BridgeAuth.cs behind a real
       HttpListener exactly as RevitMCPCore/AcadMCPApp call it. Never packaged. -->
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0-windows</TargetFramework>
    <Nullable>disable</Nullable>
    <ImplicitUsings>disable</ImplicitUsings>
    <LangVersion>7.3</LangVersion>
  </PropertyGroup>
  <ItemGroup>
    <Compile Include="..\..\bridges\sources\shared\BridgeAuth.cs" Link="BridgeAuth.cs" />
  </ItemGroup>
</Project>
