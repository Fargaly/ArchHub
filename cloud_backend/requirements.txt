fastapi==0.138.0
uvicorn[standard]==0.49.0
stripe==15.2.1
httpx==0.28.1
# pydantic relaxed from ==2.9.2: the Tests CI job co-installs this file
# with app/requirements.txt into ONE env, and specklepy there pulls
# pydantic-settings, which requires pydantic newer than 2.9.2 — the
# hard pin produced pip ResolutionImpossible and broke the whole suite.
# >=2.12.5 is the locally-proven floor (ships with specklepy 3.2.x);
# <3 caps the major. The [email] extra also covers email-validator.
pydantic[email]>=2.13.4,<3
email-validator==2.3.0
python-dotenv==1.2.2
# v1.1.0 marketplace upload endpoint uses multipart/form-data.
# FastAPI lazy-imports python-multipart on first Form()/File() touch;
# CI surfaces the missing dep as "RuntimeError: Form data requires
# python-multipart to be installed" — local dev had it transitively.
# 0.0.27 — clears 3 Dependabot alerts: arbitrary file write (<0.0.22),
# DoS via large preamble (<0.0.26), DoS via unbounded part headers
# (<0.0.27).  Bumped 2026-05-22.
python-multipart==0.0.32
# v1.1.0 marketplace signing — needed for Ed25519 signature verification
# of uploaded packs. Re-uses the same dep app/requirements.txt added.
cryptography>=49.0.0
pytest==9.1.1
pytest-asyncio==1.4.0
