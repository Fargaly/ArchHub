# JOSE verification for RFC 9449 DPoP and OIDC adapters. 1.4.2 fixed
# GHSA-frfh-8v73-gjg4; the upper bound forces an explicit major-version court.
joserfc>=1.4.2,<2
# The cloud boundary is a thin FastAPI adapter over graph-defined routes.
# Keep the major capped; route-wrapper behavior is covered by integration tests.
fastapi>=0.136.1,<1
httpx>=0.28.1,<1
# Local asymmetric signing courts and provider-neutral KMS contract tests.
# Cloud adapters remain optional integrations behind the same contract.
cryptography>=48,<49
# Shared Universal Cell authority. Credentials remain outside source and atoms.
psycopg[binary]>=3.3,<4
# Fly OIDC supplies short-lived AWS credentials; HMAC key bytes stay in KMS.
boto3>=1.43,<2
# Immutable HAMT backing for Universal Cell revision snapshots.
rpds-py>=0.30,<1
# Structured parsing of the normative Markdown specification before Cell import.
markdown-it-py>=4,<5
# The personal brain every user gets (shipped beside the launcher as personal_brain/).
pydantic>=2.0,<3
anyio>=4.0,<5
mcp>=1.27.2,<2

# The desktop window itself. The launcher imports PyQt6.QtWidgets and
# PyQt6.QtWebEngineWidgets before it can show anything, and QtWebEngine is a
# SEPARATE distribution from PyQt6 -- listing only PyQt6 left a clean machine
# with a setup that finished and an app that died on the WebEngine import,
# no window and no message.
PyQt6>=6.11,<7
PyQt6-WebEngine>=6.11,<7
# The ASGI server the universal cloud listener imports at module scope, so it
# is reached on every boot, not only when a cloud route is used.
uvicorn>=0.46,<1
# Pipeline effect engines: DXF drafting and image work. Their imports are
# guarded, so a machine without them still boots and reports the engines as
# absent; they are listed because the setup installs from this file alone.
ezdxf>=1.4,<2
numpy>=2.4,<3
opencv-python-headless>=4.13,<5
