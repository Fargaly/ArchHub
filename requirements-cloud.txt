# CLOUD requirements: the server runtime only (Universal Cell cloud gateway,
# Postgres authority, KMS). Never installed on a desktop; the desktop list is
# requirements.txt. packaging/cloud/requirements.lock is compiled from this file.
# JOSE verification for RFC 9449 DPoP and OIDC adapters. 1.4.2 fixed
# GHSA-frfh-8v73-gjg4; the upper bound forces an explicit major-version court.
joserfc>=1.4.2,<2
# The cloud boundary is a thin FastAPI adapter over graph-defined routes.
# Keep the major capped; route-wrapper behavior is covered by integration tests.
fastapi>=0.136.1,<1
httpx>=0.28.1,<1
cryptography>=48,<49
# Shared Universal Cell authority. Credentials remain outside source and atoms.
psycopg[binary]>=3.3,<4
# Fly OIDC supplies short-lived AWS credentials; HMAC key bytes stay in KMS.
boto3>=1.43,<2
# The ASGI server the universal cloud TLS listener runs on.
uvicorn>=0.46,<1
rpds-py>=0.30,<1
markdown-it-py>=4,<5
# Server-side image engine (sketch_lines); desktops report it as absent.
opencv-python-headless>=4.13,<5
