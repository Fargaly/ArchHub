# DESKTOP requirements: exactly what colleague_setup.py installs on a person's
# machine (from the installer's bundled wheelhouse, offline). The server
# packages FastAPI, psycopg, boto3 and OpenCV live in requirements-cloud.txt and
# are never installed on a desktop. uvicorn and starlette ARE installed, as
# dependencies of mcp (the personal brain), but the desktop never imports
# them; a court holds the desktop boot to that.
# JOSE verification for RFC 9449 DPoP and OIDC adapters. 1.4.2 fixed
# GHSA-frfh-8v73-gjg4; the upper bound forces an explicit major-version court.
joserfc>=1.4.2,<2
httpx>=0.28.1,<1
# Local asymmetric signing courts and provider-neutral KMS contract tests.
# Cloud adapters remain optional integrations behind the same contract.
cryptography>=48,<49
# Immutable HAMT backing for Universal Cell revision snapshots.
rpds-py>=0.30,<1
# Structured parsing of the normative Markdown specification before Cell import.
markdown-it-py>=4,<5
# The personal brain every user gets (shipped beside the launcher as personal_brain/).
pydantic>=2.0,<3
anyio>=4.0,<5
mcp>=1.27.2,<2

# The desktop window itself. The launcher imports PyQt6.QtWidgets and
# PyQt6.QtWebEngineWidgets before it can show anything, and QtWebEngine is a
# SEPARATE distribution from PyQt6 -- listing only PyQt6 left a clean machine
# with a setup that finished and an app that died on the WebEngine import,
# no window and no message.
PyQt6>=6.11,<7
PyQt6-WebEngine>=6.11,<7
# Pipeline effect engine: DXF drafting. Its import is guarded, so a machine
# without it still boots and reports the engine as absent.
ezdxf>=1.4,<2
numpy>=2.4,<3
# Windows Office host brokers attach to Classic Outlook and other Office COM hosts.
# Match the packaged Windows runtime; New Outlook does not expose this interface.
pywin32==311; sys_platform == "win32"
# Native desktop admission reads process executable, arguments and working directory.
# Ship this dependency; minimal pipe PID custody alone cannot admit the desktop.
psutil==7.2.2
# Read existing Windows Credential Locker entries through the same backend used
# by the application credential store; setup must not silently omit this reader.
keyring==25.7.0
